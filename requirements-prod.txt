# Production requirements - lightweight for Vercel deployment
# Only includes packages needed to run the Flask app with pre-computed embeddings

flask>=2.3.0
numpy>=1.24.0
scikit-learn>=1.3.0
Pillow>=10.0.0
matplotlib>=3.7.0
replicate>=0.20.0
python-dotenv>=1.0.0
requests>=2.31.0

